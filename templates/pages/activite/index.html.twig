{% extends 'base.html.twig' %}

{% block title %}AOC - Partenaire - Fiches
{% endblock %}

{% block body %}
    <div class="container mt-4">

            {% for message in app.flashes('success') %}
                <div class="alert alert-success mt-4">
                    {{ message }}
                </div>
            {% endfor %}

        <h1>Activités</h1>

                Créer une fiche
            </a>

            <table class="table table-hover">
                <thead>
                <tr>
                    <th scope="col">Numéro</th>
                    <th scope="col">Information</th>
                    <th scope="col">Edition</th>
                    <th scope="col">Suppresion</th>
                </tr>
                </thead>
                <tbody>
                {% for activite in activites %}
                    <tr class="table-primary">
                        <th scope="row">{{ activite.id }}</th>
                        <td><strong> Nom : </strong></br> {{ activite.nom }}
                            </br> <strong> Description : </strong></br> {{ activite.description }}
                            </br> <strong> Icon : </strong></br> {{ activite.icon }}</td>
                        <td>
                            <a href="{{ path('app_activite_edit', { id: activite.id }) }}" class="btn btn-info">Modifier</a>
                        </td>
                        <td>
                            <a href="{{ path('app_activite_delete', { id: activite.id }) }}" class="btn btn-warning">Supprimer</a>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>



    </div>
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}{{ activite.nom }}{% endblock %}

{% block body %}
<link rel="stylesheet" href="../style.css">
    <nav class="contentUl">
        <h1 class="titleform" style="font-size=25px;">{{ activite.nom }}</h1>
        {% if is_granted('ROLE_ADMIN') %}
        <a href="{{ path('app_activite_edit', { id: activite.id }) }}"><span class="material-symbols-outlined">edit_square</span></a>
        {% endif %}
        <dl>
            <dt class="titleform">Icone</dt>
            <dd class="textform">{{ activite.icon }}</dd></center>
            <dt class="titleform">Description</dt>
            <dd class="textform">{{ activite.description }}</dd></center>
        </dl>
    </nav>

{% endblock %}

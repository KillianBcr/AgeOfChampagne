<style>
.nav-link{
    color:#f2f4f3;
    transition: .1s ease-in-out;
}
.nav-link:hover{
    color:grey;
}
.container-fluid{
    background-color: #07080a;
}
.navbar{
        --bs-navbar-padding-x:none;
}
.navbar-toggler-icon{
    filter: invert(100%);
}
.title{
    font-size:28px;
    text-decoration:none;
    font-family: serif;
    font-weight:bold;
}
.navbar-brand{
    font-size:18px;
}
    #admin{
        height: 55px;
        top:80px;
        left:5%;
        transition: .1s ease-in-out;
    }
    #admin:hover{
        height: 58px;
    }
</style>

<nav class="navbar navbar-expand-lg bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" style="color:#f2f4f3;" href="/">Acceuil</a>
        <a class="title" style="color:#f2f4f3;" href="/">Age of Champagne</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="mailto: aoc.le.jeu@gmail.com">Contact</a>

                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <a class="nav-link" href="{{ path('app_partenaire') }}">Partenaires</a>
                {% endif %}
                {% if is_granted('ROLE_ADMIN') %}
                    <a href="?"><img id="admin" src="img/admin.png" alt="gestion admin"> </a>
                {% endif %}
            </div>
            <div class="d-flex">
                <ul class="navbar-nav me-auto">
                    {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                        <li class="nav-item dropdown">
                            <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">{{ app.user.nom }} {{ app.user.prenom }}</a>
                            <div class="dropdown-menu">
                                <a class="dropdown-item" href="{{ path('app_profile', {id : app.user.id}) }}">Modifier mes informations</a>
                                <a class="dropdown-item" href="{{ path('app_edit_password', {id : app.user.id}) }}">Modifier mon mot de passe</a>
                                <div class="dropdown-divider"></div>
                                {% if is_granted('ROLE_ADMIN') %}
                                    <a class="dropdown-item" href="{{ path('admin') }}">Administration</a>
                                    <div class="dropdown-divider"></div>
                                {% endif %}
                                
                                {% if is_granted('ROLE_ADMIN') %}
                                    <a class="dropdown-item" href="{{ path('app_fiche') }}">Ma fiche partenaire</a>
                                    <div class="dropdown-divider"></div>
                                {% endif %}

                                <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
                            </div>
                        </li>
                    {% else %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_login') }}">Connexion
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_register') }}">Inscription
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </div>
</nav>
